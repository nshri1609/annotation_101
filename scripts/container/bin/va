#!/usr/bin/env bash
set -euo pipefail

# Run from the repo root so `uv run` finds pyproject.toml.
for candidate in /workspaces/VideoAnnotator /app; do
  if [[ -f "$candidate/pyproject.toml" ]]; then
    cd "$candidate"
    break
  fi
done

exec uv run videoannotator "$@"
